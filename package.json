{
  "name": "firewifi-dashboard",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web-ui run dev",
    "build": "./scripts/build-ui.sh",
    "typecheck": "npm --prefix web-ui run typecheck",
    "lint": "npm --prefix web-ui run lint",
    "check": "npm --prefix web-ui run check"
  }
}
